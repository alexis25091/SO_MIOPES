/* Equipo: Sin nombre    18/09/24   version 2*/
/* le quitamos un for repetido, y algunos errores de sintaxis */

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

int main (int argc, char *argv[]){
    int i;
    printf("Ejecutando el programa invocador (execprog1). Sus argumentos son: \n");
    for (i=0;i<=argc; i++){
        print("argv[%d]: %s\n", i, argv[i]);
        sleep(10);
        strcpy(argv[0], "execprog2");
    }
    if (execv ("•/execprog2", argv)< 0){
        printf("Error en la invocacion a execprog2 \n");
        exit (1);
    }
    exit(0);
}
